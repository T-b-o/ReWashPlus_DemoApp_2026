@page "/admin/manage-payments"
@using ReWashPlus_DemoApp.Services
@inject ReWashPlus_DemoApp.Services.PageTitleService PageTitleService

<div class="p-6">
    <h1 class="text-2xl font-bold text-clickup-accent">Manage Payments</h1>
    <p class="text-sm text-clickup-muted">Admin payments management UI.</p>
</div>

@code {
    protected override void OnInitialized()
    {
        PageTitleService.TitleChanged += () => InvokeAsync(StateHasChanged);
    }
}