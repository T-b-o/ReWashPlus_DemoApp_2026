@page "/payments"
@using ReWashPlus_DemoApp.Services
@inject PageTitleService PageTitleService

<div class="p-6 bg-clickup-dark min-h-screen text-clickup-text">
    <h1 class="text-2xl font-bold text-clickup-accent">Payments</h1>
    <p class="text-sm text-clickup-muted">This is the payments overview. Implement payment list and actions here.</p>
</div>

@code {
    protected override void OnInitialized()
    {
        PageTitleService.TitleChanged += () => InvokeAsync(StateHasChanged);
    }
}